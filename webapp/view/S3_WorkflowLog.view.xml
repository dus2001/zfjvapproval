<mvc:View xmlns="sap.m" xmlns:core="sap.ui.core" xmlns:mvc="sap.ui.core.mvc"
	controllerName="edu.weill.cornell.wbg.fjv.approve.controller.S3_WorkflowLog">
	<Text text="Journal Voucher Approval changes dynamically depending on approvals, and change change the route."/>
	<Table noDataText="No Data" id="WFLog" items="{ path: '/TASKDTL_WI/results' , sorter : { path:'Zzstplvlkey' , group:true} }">
		<items>
			<ColumnListItem>
				<cells width="3rem">
					<core:Icon src="{ path: 'Zjvwflvlst',formatter: '.formatter.WorkflowLogIcon'}"
						tooltip="{ path: 'Zjvwflvlst',formatter: '.formatter.WorkflowLogToolTip'}"/>
				</cells>
				<cells>
					<Text text="{Zjvapprtyt}"/>
				</cells>
				<cells>
					<Text text="{ZounameSD}"/>
				</cells>
				<cells visible="{= !(${Zcreat} === '0')}">
					<HBox>
						<Text text="{Emnam}" visible="{= !(${Zcreat} === '0')}"/>
						<Link text="{i18n>apprLinkText}" press="handleShowApprovers" visible="{= ${Zcreat} === '0'}"/>
					</HBox>
				</cells>
				<cells>
					<!--<Text text="{ path:'Zcreat', formatter:'.formatter.formatTimeStamp'}"/>-->
					<!--<Text text=" {path : 'Zcreat', type : new sap.ui.model.type.DateTime, formatOptions: { style : 'medium'}} "/>-->
					<!--<Text text="{Zcreat}" visible="{= !(${Zcreat} == '0') }"/>-->
					<Text text="{ApproveAt}" visible="{= !(${Zcreat} === '0')}"/>
				</cells>
			</ColumnListItem>
		</items>
		<columns>
			<Column width="3rem">
				<header><Label text=""/></header>
			</Column>
			<Column>
				<header><Label text="{i18n>aprTypeLbl}"/></header>
			</Column>
			<Column>
				<header><Label text="{i18n>apprDeptLbl}"/></header>
			</Column>
			<Column>
				<header><Label text="{i18n>apprNameLbl}"/></header>
			</Column>
			<Column>
				<header><Label text="{i18n>apprTimeLabel}"/></header>
			</Column>
		</columns>
	</Table>
</mvc:View>